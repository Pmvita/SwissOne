{
  "installCommand": "cd ../.. && npm install --legacy-peer-deps",
  "buildCommand": "npm run build",
  "crons": [
    {
      "path": "/api/cron/fetch-prices",
      "schedule": "0 9 * * *"
    }
  ]
}

